# Issue #172 - Native semantic state ownership

- Date: 2026-09-21
- Scope: remove staged min-cost-flow state tracking from the TypeScript facade
  after establishing the native mutation policy in #166.
- Acceptance: native code is the sole owner of staged-flow availability and
  invalidation; facade validation and stable error normalization remain.
- Decision: delegate `pushRelabelMaximumFlow()` and `cycleCanceling()` directly
  to the raw graph, retain no facade mutation counters, and decode/release
  native exceptions through exported Emscripten runtime helpers.
- Files: runtime-neutral TypeScript facade, generated `wasm/dist` outputs, Node
  flow contract, architecture documentation, changelog, and session records.
- Verification: TypeScript build, rebuilt Node contract, npm-pack consumer,
  semantic-state symbol audit, and diff checks passed.
- Next: commit, push, confirm CI, close #166 and #172, and update the 1.0
  roadmap.
