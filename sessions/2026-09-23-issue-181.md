# Issue #181 - Consolidate WASM documentation

## Scope and acceptance

Make the WASM package README a practical user entrypoint with representative
API examples. Keep one authoritative architecture guide and one maintainer
build/release guide, without duplicated method inventories or obsolete plans.

## Decisions

- Merge the 1.0 decision record into `wasm/ARCHITECTURE.md`, retaining the
  migration rationale and completion gates but linking to the issue roadmap
  instead of copying its issue list.
- Remove `wasm/ARCHITECTURE_1_0.md` and the superseded
  `wasm/ARCHITECTURE_PLAN.md`; Git history retains both.
- Keep `wasm/README.md` consumer-oriented and use repository URLs for related
  documents absent from the npm tarball. Keep build, CI, troubleshooting, and
  release procedure in `wasm/WASM.md`.
- Make no source, package-version, or publication changes.

## Changed files

- `wasm/README.md`, `wasm/WASM.md`, `wasm/ARCHITECTURE.md`, and root `README.md`.
- Removed `wasm/ARCHITECTURE_1_0.md` and `wasm/ARCHITECTURE_PLAN.md`.
- Updated `CHANGELOG.md` and `SESSIONS.md`.

## Verification

- All five JavaScript README examples executed against the installed local
  tarball: passed for paths, attributes, traversal, components, multigraph
  edge IDs, and maximum flow.
- Checked 32 local Markdown links: passed.
- `npm --prefix wasm run build:types`: passed.
- Packed external consumer and public declaration check with the existing
  runtime asset: passed.
- `git diff --check`: passed.

## Next action

Commit and push the change, then confirm relevant CI before closing #181.
Resume the #177 release-readiness audit as a separate task.
