# Issue #180 - Remove the unused WASM singleton

## Scope and acceptance

Remove the unpublished legacy singleton TypeScript source and its tracked
compiled output. Keep the explicit `createNxpp()` package root and the npm
tarball contract unchanged.

## Decisions

- Repository searches found no active source, test, example, or package import
  of the legacy path; the package allowlist already excluded it.
- Keep the historical `0.6.0` baseline description while correcting current
  architecture and migration wording.
- No C++ or Embind rebuild is needed because only unused TypeScript source and
  compiled output changed.

## Changed files

- Removed `wasm/ts/legacy/` and `wasm/dist/legacy/`.
- Updated `wasm/ARCHITECTURE.md`, `wasm/ARCHITECTURE_1_0.md`,
  `CHANGELOG.md`, and `SESSIONS.md`.

## Verification

- `npm --prefix wasm run build:types`: passed.
- Clean TypeScript build to a temporary directory: matched `wasm/dist` exactly
  and emitted no legacy output.
- Node contract with `NXPP_WASM_NODE_CONTRACT_SKIP_BUILD=1`: passed.
- npm-pack consumer with `NXPP_WASM_NPM_PACK_SKIP_BUILD=1`: passed, including
  exact tarball contents and public root exports.
- `git diff --check`: passed.

## Next action

Commit and push the change, confirm relevant CI, then close #180 and mark it
complete in #177. Treat Node-version validation in #179 as a separate task.
