# Issue #166 - Staged flow mutation semantics

- Date: 2026-09-21
- Scope: invalidate native staged min-cost-flow state only after graph data is
  successfully changed.
- Acceptance: rejected mutations and documented no-ops preserve staged state;
  successful structural, weight, and attribute mutations invalidate it.
- Decision: validate or detect no-ops before invalidation, then invalidate
  after successful writes, including node and edge attribute proxy writes.
- Files: native graph, multigraph, and attribute headers; focused flow tests;
  changelog, session index, and this record.
- Verification: focused `test_flow` binary, full native suite (106/106), and
  diff checks passed.
- Next: commit the native semantics, then remove facade-side state tracking in
  #172 before closing the cross-runtime work.
