# Issue #165 - WASM generated facade verification

- Date: 2026-09-21
- Scope: make TypeScript compilation and tracked `wasm/dist` alignment required
  checks in experimental WASM CI and tag-driven WASM releases.
- Acceptance: use locked npm dependencies, rebuild the facade, fail on a dirty
  generated-artifact diff, and retain Node contract and package-consumer checks.
- Decision: use Node.js 24 in the experimental lane to match the release lane,
  then compare only `wasm/dist` after `build:types`.
- Files: both WASM workflows, `CHANGELOG.md`, `SESSIONS.md`, and this record.
- Verification: npm clean install, TypeScript build, clean `wasm/dist` diff,
  Node contract, npm-pack consumer, workflow diff inspection, and CI.
- Next: commit, push, confirm CI, and close issue #165.
