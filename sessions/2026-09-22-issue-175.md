# Issue #175 - Environment-specific WASM loaders

- Date: 2026-09-22
- Scope: separate runtime-neutral facade construction from Node and browser
  module initialization without duplicating bindings or graph behavior.
- Acceptance: keep the generated Node artifact import inside the Node adapter,
  reuse the shared runtime context from the browser demo, and verify browser
  asset lookup through a bounded smoke path.
- Decision: use `NxppRuntimeLoader` and `createNxppWithLoader()` as the common
  initialization boundary. Keep `createNxpp()` as the intended Node-facing
  API, not as a compatibility shim. Do not add aliases for the experimental
  `0.6` surface; package export cleanup remains in #176.
- Files: runtime context and Node/browser adapters, generated TypeScript
  declarations, loader contract tests, browser demo and smoke script,
  experimental WASM CI, architecture documentation, changelog, and ignore
  rules for generated browser artifacts.
- Verification: TypeScript build, isolated loader contract, fresh Node contract
  suite, npm-pack consumer test, browser build and headless facade smoke test,
  shell syntax checks, and `git diff --check` passed.
- Next: review and commit the focused change, confirm the experimental WASM CI,
  close #175, then start package-layout issue #176.
