# Issue #171 - Explicit WASM runtime context

- Date: 2026-09-21
- Scope: replace the implicit singleton in the target WASM architecture with
  an explicit context without changing the current package-root API.
- Acceptance: create all eight graph classes from a context, support isolated
  concurrent contexts, preserve subgraph ownership and disposal, forward
  module options, and report initialization failures clearly.
- Decision: keep runtime-neutral facade factories under `wasm/ts/core`, the
  Node context under `wasm/ts/runtime`, and thin singleton adapters under
  `wasm/ts/legacy` until the 1.0 export cutover.
- Files: TypeScript facade/runtime sources, generated `wasm/dist` outputs,
  compile and Node contract tests, architecture documents, changelog, session
  index, and this record.
- Verification: TypeScript build, Node contract tests with two contexts,
  npm-pack consumer test, and diff checks.
- Next: commit, push, confirm CI, close #171, then continue with #172.
