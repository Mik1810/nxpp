# Issue #176 - Minimal WASM package layout

- Date: 2026-09-23
- Scope: replace the experimental singleton package surface with the explicit
  runtime-context API and publish only intentional facade dependencies and
  Node runtime assets.
- Acceptance: provide one typed root export, resolve the adjacent WASM asset
  after installation, reject unsupported subpaths, and verify an exact tarball
  layout from a clean external ESM consumer.
- Decision: make `createNxpp()` the only JavaScript export; intentionally remove
  the default singleton, global constructors, `loadNxppRuntime()`, and the
  `./runtime` shim. Keep raw Embind and browser adapters internal. Store the
  generated Node module and WASM binary under `wasm/runtime/`.
- Files: package metadata, public facade entrypoint, generated facade and Node
  assets, build and consumer scripts, contract fixtures, examples, benchmarks,
  CI and release verification, package documentation, changelog, and session
  index.
- Verification: TypeScript build, 28-file package dry-run, fresh Emscripten
  build, full Node API contract, external npm-pack runtime and type consumer,
  unsupported-subpath assertions, shell syntax checks, and `git diff --check`
  passed.
- Next: review and commit the focused change, confirm experimental and release
  artifact checks in CI, close #176, then retire the stale checklist in #168.
- CI follow-up: the first run built and tested successfully but compared its
  Boost 1.83 output byte-for-byte against artifacts built with Boost 1.90.
  Both WASM workflows now use the supported Boost 1.86 headers and compare two
  builds made in the same runner. Await the rerun before closing #176.
