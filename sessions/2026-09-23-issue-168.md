# Issue #168 - Retire stale WASM checklist

- Date: 2026-09-23
- Scope: reconcile the former `wasm/TODO.md` with current bindings, facade,
  contracts, package layout, and browser investigation; keep GitHub issues as
  the only actionable WASM roadmap.
- Acceptance: completed work is no longer listed as pending, remaining work has
  an issue, Node/browser support descriptions agree, and documentation links
  resolve.
- Decision: retain `TODO.md` only as a pointer for old links. Graph core,
  typed facades, active algorithm slices, error and lifetime contracts, and
  packaging already have implementation and verification. Topological sort,
  generators, SAT, full native parity, and a different ABI are outside the
  declared `1.0.0` scope. Publication is handled by the release workflow.
- Follow-ups: issue #179 tracks the gap between `engines.node >=20` and the
  Node 24-only package CI. Issue #180 tracks unused legacy singleton source and
  generated output. Both are linked from roadmap #177.
- Files: WASM TODO, architecture and API policy, package and root READMEs,
  WASM technical overview, 1.0 architecture, changelog, and session index.
- Verification: documentation link check and `git diff --check` passed.
- Next: commit and confirm documentation CI, close #168, then complete #179
  and #180 before the `1.0.0` release audit.
