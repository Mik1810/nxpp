# Issue #173 - WASM bridge DTO and ownership boundaries

- Date: 2026-09-22
- Scope: standardize active raw graph results as JavaScript primitives, arrays,
  and plain DTOs, with explicit ownership limited to graph handles.
- Acceptance: document every active result family and owner, remove temporary
  Embind result wrappers, make raw TypeScript shapes concrete, and cover value
  shape, ownership, empty results, and existing error behavior.
- Decision: preserve the experimental `0.6` facade endpoint methods through a
  mechanical JavaScript adapter while changing the internal endpoint result to
  `{ source, target }`; graph and subgraph handles remain explicitly disposable.
- Files: bridge binding utilities and multigraph registration, raw TypeScript
  declarations and adapters, generated facade artifacts, bridge documentation,
  Node contract tests, changelog, and session index.
- Verification: TypeScript build and all WASM C++ translation-unit syntax
  checks passed. A fresh Emscripten module build, the Node contract, and the
  npm-pack consumer test passed. JavaScript syntax checks and `git diff --check`
  passed after restoring the generated `wasm/build` outputs.
- Next: review and commit the focused change, then close #173 after CI succeeds.
