# Issue #178 - Legacy TypeScript facade boundary

- Date: 2026-09-21
- Scope: isolate the current `0.x` TypeScript facade and singleton loader so
  the target 1.0 runtime and facade can be developed on clear paths.
- Acceptance: preserve root named and default exports, all eight graph-class
  contracts, Node behavior, lifetime semantics, and npm consumer behavior.
- Decision: move only the current loader and facade implementation under
  `wasm/ts/legacy`; keep types, converters, raw declarations, bindings, tests,
  and generated runtime assets shared.
- Files: TypeScript facade sources, generated `wasm/dist` outputs, WASM
  architecture documents, `CHANGELOG.md`, `SESSIONS.md`, and this record.
- Verification: TypeScript build, generated-path inspection, Node contract,
  npm-pack consumer, and diff checks.
- Next: commit, push, confirm CI, close #178, then implement #171 on the freed
  runtime and core paths.
